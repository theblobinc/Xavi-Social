# Secrets / local overrides
.env
.env.*
!/docker/compose/**/.env.example

# Runtime docker volumes (do not commit)
/docker/data/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/

# Logs / caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Language server / lint caches
.clj-kondo/
.lsp/

# Node build artifacts
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/

# Root-level installs (not tracked)
/node_modules/
/vendor/

# Vite build output (if generated in package root)
/dist/

# Sensitive local-only materials (never commit)
/sample-code/
/_private/

# Jetstream runtime deps (not tracked)
/jetstream/node_modules/
/jetstream/.package-lock.json